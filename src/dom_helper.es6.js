class DOMHelper{
  constructor(tag, props, children) {
    this.tagName = tag;
    this.attr = attr;
    this.children = children;

  }

  create(){
    this.node = document.createElement(type);
    return this.node;
  }

  render(){
    return 
  }
}

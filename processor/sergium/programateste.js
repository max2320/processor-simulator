$(document).ready(function(){
    
    var i=0;
    //pega valor per
    valor('em',i++);
    valor('mem',0);

    valor('em',i++); 
    valor('mem',0);
    //END pega valor per
    
    
    valor('em',i++);
    valor('mem',5);

    valor('em',i++);
    valor('mem',18);

    valor('em',i++);
    valor('mem',3);

    valor('em',i++);
    valor('mem',0);

    valor('em',i++);
    valor('mem',0);

    valor('em',i++);
    valor('mem',0);

    valor('em',i++);
    valor('mem',5);

    valor('em',i++);
    valor('mem',19);

    valor('em',i++);
    valor('mem',10);

    valor('em',i++);
    valor('mem',0);

    valor('em',i++);
    valor('mem',1);

    valor('em',i++);
    valor('mem',1);

    valor('em',i++);
    valor('mem',23);

    valor('em',i++);
    valor('mem',0);
    new_periferico('numberkeypad');
    new_periferico('display');
});